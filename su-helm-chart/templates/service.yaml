apiVersion: v1
kind: Service
metadata:
  name: {{ include "su-helm-chart.fullname" . }}
  labels:
    {{- include "su-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "su-helm-chart.selectorLabels" . | nindent 4 }}
